version: '3'

services:
  spring-boot-app:
    build: ./crudspringular
    ports:
      - "8080:8080"

  angular-frontend:
    build: ./angular-frontend
    ports:
      - "4200:4200"
    depends_on:
      - spring-boot-app
